# 프로젝트 작업 지침

## 기본 원칙

- 사용자의 요청을 무조건 옳다고 가정하지 않는다. 코드, 문서와 실제 검증 결과를 근거로 객관적으로 판단한다.
- 요청에 기술적 오류, 기존 설계와의 충돌, 보안 또는 운영상 위험이 있으면 구체적인 이유와 대안을 설명한다.
- 요구사항, 도메인 정책, 공개 API 계약과 데이터 모델처럼 결과에 큰 영향을 주는 사항이 모호하면 임의로 결정하지 않고 사용자에게 질문한다.
- 결과에 영향을 주지 않는 사소한 구현 세부사항은 기존 코드와 문서의 일관된 관례를 따른다.
- 요청 범위를 벗어난 기능 추가, 대규모 리팩터링과 정책 변경은 하지 않는다.
- 확인하지 않은 사실, 실행하지 않은 테스트와 검토하지 않은 대안을 만들어 기록하지 않는다.

## 작업 전 확인

- 변경 전에 관련 코드, 테스트와 문서를 먼저 확인한다.
- 저장소의 기존 변경사항을 보존하고 작업과 무관한 파일을 수정하지 않는다.
- 공용 요구사항과 도메인 정책은 `getddo-spec`을 원본으로 사용하며 이 저장소에 중복해서 작성하지 않는다.
- 문서 내용이 충돌하면 `getddo-spec/AGENTS.md`에 정의된 다음 순서로 확인한다.
  1. 확정된 요구사항과 구현 범위
  2. 확정된 도메인 규칙
  3. 채택된 공용 결정 기록
  4. 공용 협업 규칙
  5. 미결정 사항
- `getddo-spec/00-requirements/pending-decisions.md`의 항목과 검토 중인 내용은 확정된 정책으로 해석하거나 구현에 임의로 반영하지 않는다.
- 사용자 요청과 확정된 문서가 충돌하거나 문서만으로 충돌을 해결할 수 없으면 변경 전에 사용자에게 알리고 확인한다.
- 용어는 `getddo-spec/02-domain/glossary.md`를 기준으로 통일한다.
- 구현 및 제외 범위는 `getddo-spec/00-requirements/scope.md`를 확인한다.

## 프로젝트 구조

이 프로젝트는 Java 21과 Spring Boot 기반의 멀티모듈 프로젝트다.

- `api`: 애플리케이션 실행, HTTP 요청·응답과 Controller
- `core`: 도메인 객체, 비즈니스 규칙, Service와 Repository 인터페이스
- `storage:db`: JPA Entity, 영속성 구현과 도메인-Entity 변환

다음 의존 방향을 지킨다.

```text
api → core
api → storage:db
storage:db → core
```

- `core`에서 `api` 또는 `storage:db`를 참조하지 않는다.
- HTTP DTO와 JPA Entity를 `core`의 비즈니스 모델로 직접 사용하지 않는다.
- Controller는 비즈니스 규칙을 구현하지 않고 Service에 위임한다.
- 영속성 구현은 `storage:db`에 두고 `core`는 Repository 인터페이스에만 의존한다.
- 자세한 패키지 배치 기준은 루트 `README.md`를 따른다.

## 구현 규칙

- 새로운 구현보다 기존 코드와 프로젝트 관례의 재사용을 우선한다.
- 불필요한 추상화, 범용화와 미래 요구사항을 가정한 구현을 피한다.
- 예외 처리는 기존 `ErrorCode`와 `BusinessException` 구조를 우선 사용한다.
- API 응답은 기존 `ResponseEnvelope` 형식을 따른다.
- 외부에 노출되는 API나 데이터 형식을 변경할 때는 호환성과 프론트엔드 영향을 확인한다.
- DB 스키마, 트랜잭션과 동시성 처리는 이 저장소에서 관리하되 공용 도메인 정책과 일치시킨다.
- DB 스키마 변경은 기존 Flyway 마이그레이션을 수정하지 않고 새로운 마이그레이션으로 추가한다.
- 운영 코드에 임시 로그, 디버깅 출력과 하드코딩된 비밀값을 남기지 않는다.

## 테스트 및 검증

- 동작을 변경하면 해당 동작을 검증하는 테스트를 추가하거나 수정한다.
- 버그 수정 시 가능하면 문제를 재현하는 회귀 테스트를 추가한다.
- 변경 범위에 맞는 테스트를 먼저 실행하고 완료 전 전체 테스트와 빌드를 실행한다.

```bash
./gradlew test
./gradlew build
```

- 문서만 변경한 경우에는 테스트 대신 문서 형식, 링크와 diff를 검증할 수 있다.
- 테스트나 빌드를 실행하지 못했으면 그 이유를 결과 보고에 명시한다.
- 일부 테스트 성공을 전체 빌드 성공으로 표현하지 않는다.
- 실패한 테스트를 삭제하거나 비활성화하여 검증을 우회하지 않는다.

## 문서 관리

- 백엔드 내부 구조, DB, API 구현 세부사항, 테스트, 빌드, 배포와 장애 대응은 이 저장소의 `docs/`에서 관리한다.
- 작은 수정은 PR 설명만으로 충분하며 모든 작업에 별도 문서를 만들지 않는다.
- 구현 과정, 기술적 선택이나 검증 결과를 다음 작업에서 참고해야 할 때 `docs/05-worklogs`에 작업 기록을 작성한다.
- 여러 작업에 영향을 주거나 변경하기 어려운 백엔드 기술 결정은 `docs/04-decisions`에 ADR로 기록한다.
- 배포, 장애 확인 또는 복구 절차가 변경되면 `docs/06-runbook`을 갱신한다.
- 공용 정책 변경은 이 저장소에서 확정하지 않고 `getddo-spec`의 요구사항, 도메인 문서, 공용 ADR과 미결정 사항 관리 규칙을 따른다.
- 작업 기록과 ADR은 `docs/templates`의 양식을 사용한다.
- AI가 작성한 작업 기록은 `검토 대기`, ADR은 `제안` 상태로 시작하며 담당자 확인 없이 완료 또는 채택으로 표시하지 않는다.
- 과거 작업 기록은 확정 정책의 근거로 사용하지 않고 현재 코드와 기준 문서를 다시 확인한다.
- 비밀번호, 토큰과 개인정보를 문서에 기록하지 않는다.

## Git 및 협업

- 브랜치, 커밋, 작업 흐름과 Pull Request 규칙은 `getddo-spec/01-conventions`를 따른다.
- 커밋 메시지는 `type: 작업 내용` 형식을 사용한다.
- 사용자의 명시적인 요청 없이 커밋, push, 브랜치 생성·변경과 Pull Request 생성을 하지 않는다.
- `main`과 `dev`에 직접 push하지 않는다.
- 작업 브랜치는 `dev`에서 분기하며 `<타입>/<Jira 이슈 키>` 형식을 사용한다.
- PR은 `dev`를 대상으로 하고 최소 1명 이상의 리뷰와 CI 검증 후 병합한다.

## 금지 사항

사용자의 명시적인 요청과 필요한 정책 확인 없이 다음 작업을 하지 않는다.

- 기존 데이터 또는 파일 삭제
- 기존 Flyway 마이그레이션 수정
- 의존성 또는 플러그인의 메이저 버전 변경
- 공개 API 계약이나 확정된 도메인 정책 변경
- 대규모 리팩터링
- 테스트 실패를 숨기기 위한 테스트 비활성화

## 작업 완료 보고

완료 시 다음 내용을 실제 수행 결과에 근거해 간결하게 보고한다.

- 변경한 내용과 이유
- 실행한 테스트 또는 검증과 실제 결과
- 실행하지 못한 검증과 이유
- 남아 있는 위험, 미결정 사항과 후속 작업
